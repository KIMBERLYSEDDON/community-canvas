
     <h1 class="title is-3 is-left">Featured Image:</h1>
    <div class="card m-3 featured">
      <div class="card-image">
        <figure class="image">
          <img id="img" data-img-id="{{post.image}}" src="{{post.image}}" alt="Placeholder image">
        </figure>
      </div>
        <div class="card-content" style="margin-bottom: 1.5rem; padding: 0rem;">
          <div class="content columns">
            <p class="has-text-left column">Posted by: {{post.user.username}} </p>
            <p class="has-text-right column">On: {{format_date post.date_posted}} </p>
            <br>
          </div>
      </div>
    </div>
